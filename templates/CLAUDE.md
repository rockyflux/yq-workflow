# templates Guide

`templates/` stores static assets installed into the user environment.

## Included Assets

- `prompts/claude/`
- `skills/`
- `rules/`
- `output-styles/`

## Current Direction

- Command files are generated from the workflow registry at install time.
- Template assets should stay Claude-oriented and should not assume multi-model routing.
